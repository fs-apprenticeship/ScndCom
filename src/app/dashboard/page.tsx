import { VoiceDashboard } from "./_components/voice-dashboard";

/** Provides the App Router entry point for the client-side voice dashboard. */
export default function DashboardPage() {
  return <VoiceDashboard />;
}
